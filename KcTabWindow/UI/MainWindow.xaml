<ui:FluentWindow
    x:Class="KcTabWindow.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:KcTabWindow.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="课程表"
    Width="1265"
    Height="650"
    d:DataContext="{d:DesignInstance Type=local:MainWindow}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Closing="FluentWindow_Closing"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="None"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:TitleBar
                x:Name="TitleBar"
                Title="课程表"
                Grid.Row="0"
                Grid.Column="0"
                CloseWindowByDoubleClickOnIcon="True">
                <ui:TitleBar.Icon>
                    <ui:ImageIcon Source="/Assets/wpfui-icon-256.png" />
                </ui:TitleBar.Icon>
            </ui:TitleBar>
            <ui:NavigationView
                x:Name="RootNavigation"
                Grid.Row="1"
                Grid.Column="0"
                IsBackButtonVisible="Visible"
                Loaded="RootNavigation_Loaded"
                PaneDisplayMode="LeftFluent">
                <ui:NavigationView.Header>
                    <ui:BreadcrumbBar
                        Margin="42,32,0,0"
                        FontSize="28"
                        FontWeight="DemiBold" />
                </ui:NavigationView.Header>
                <ui:NavigationView.MenuItems>
                    <ui:NavigationViewItem
                        x:Name="TableView"
                        Content="课表"
                        IsEnabled="False"
                        TargetPageType="{x:Type local:TablePage}">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="Table24" />
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                    <ui:NavigationViewItem Content="登陆" TargetPageType="{x:Type local:LoginPage}">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="Person24" />
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                </ui:NavigationView.MenuItems>

                <ui:NavigationView.FooterMenuItems>
                    <ui:NavigationViewItem Content="设置" TargetPageType="{x:Type local:SettingPage}">
                        <ui:NavigationViewItem.Icon>
                            <ui:SymbolIcon Symbol="Settings24" />
                        </ui:NavigationViewItem.Icon>
                    </ui:NavigationViewItem>
                </ui:NavigationView.FooterMenuItems>
            </ui:NavigationView>
        </Grid>
    </Grid>
</ui:FluentWindow>
