<Grid
    x:Class="KcTabWindow.UI.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:ui1="clr-namespace:KcTabWindow.UI"
    d:DesignHeight="450"
    d:DesignWidth="600"
    mc:Ignorable="d">
    <Grid.Resources>
        <ui1:NotEmptyMultiConverter x:Key="TextToBoolConverter" />
    </Grid.Resources>
    <Grid.RowDefinitions>
        <RowDefinition Height="auto" />
        <RowDefinition Height="auto" />
        <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock
        Grid.Row="0"
        Margin="26,0,26,0"
        HorizontalAlignment="Center"
        FontSize="36"
        FontWeight="Bold"
        Foreground="White"
        Text="导入数据" />
    <StackPanel
        Grid.Row="1"
        Margin="16,0,16,0"
        Orientation="Vertical">
        <ui:TextBox
            x:Name="account"
            Grid.Column="0"
            Margin="0,26,0,0"
            VerticalAlignment="Center"
            PlaceholderText="输入Token" />
    </StackPanel>
    <Grid Grid.Row="3" Margin="6,16,16,16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <CheckBox
            x:Name="checkbox"
            Grid.Row="2"
            VerticalAlignment="Bottom">
            <CheckBox.Content>
                <StackPanel Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center" Text="请阅读" />
                    <ui:HyperlinkButton
                        VerticalAlignment="Center"
                        VerticalContentAlignment="Center"
                        Content="《软件协议》"
                        NavigateUri="https://www.baidu.com" />
                    <TextBlock VerticalAlignment="Center" Text="后同意" />
                </StackPanel>
            </CheckBox.Content>
        </CheckBox>
        <ui:Button
            Name="StartButton"
            Grid.Column="1"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Content="导入所有">
            <Button.IsEnabled>
                <MultiBinding Converter="{StaticResource TextToBoolConverter}">
                    <Binding
                        ElementName="account"
                        Path="Text"
                        UpdateSourceTrigger="PropertyChanged" />
                    <Binding
                        ElementName="checkbox"
                        Path="IsChecked"
                        UpdateSourceTrigger="PropertyChanged" />
                </MultiBinding>
            </Button.IsEnabled>
        </ui:Button>
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Grid>
                <Line
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Stroke="DarkGray"
                    StrokeThickness="1"
                    X1="0"
                    X2="200"
                    Y1="0"
                    Y2="0" />
                <TextBlock
                    Padding="13,0,13,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
<<<<<<< HEAD
=======
                    Background="{StaticResource Back}"
>>>>>>> b8049479c3bab8bf10ff41db05d567dbf2c6dee3
                    Foreground="White"
                    Text="其他登陆方式" />
            </Grid>
            <ui:Button
                Name="KcZipButton"
                HorizontalAlignment="Center"
                Content=".kczip" />
        </StackPanel>
    </Grid>
</Grid>